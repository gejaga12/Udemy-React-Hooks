const activo = true;

// let msj = "";

// if (!activo) {
//   msj = "Activo";
// } else {
//   msj = "Inactivo";
// }

// const msj = (activo) ? "Activo" : "Inactivo";
// const msj = (activo) ? "Activo" : null;

const msj = activo && "Activo";

console.log(msj);
